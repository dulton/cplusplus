int main(int argc, char* argv[])
{
/*	m_thdPool.Start(num);//num=cpu+2

	//向线程池中添加任务
	m_thdPool.AddTask([handler,bufPtr, len]{
                if(!handler->IsValid())
                {
                    return;
                }

                boost::system::error_code ec;
                write(handler->GetSocket(), buffer(bufPtr.get(), len), ec);
            });*/

    return 0;
}